# Dockerfile.userservice
FROM openjdk:17-slim AS builder
WORKDIR /app
COPY pom.xml .
COPY src ./src
COPY .mvn/ .mvn/
COPY mvnw .
RUN chmod +x mvnw
RUN ./mvnw clean package -DskipTests

FROM openjdk:17-slim
WORKDIR /app
COPY --from=builder /app/target/supply_chain-0.0.1-SNAPSHOT.jar app.jar
ENV PORT=8081 \
    MONGO_URI=mongodb+srv://admin:admin@supplychain.fkxkn.mongodb.net/ \
    MONGO_DB=supply_chain
EXPOSE ${PORT}
ENTRYPOINT ["java", "-jar", "app.jar", "--spring.profiles.active=userservice"]